// instead of hard coding credentials and timeouts, put them in a config file 
//so you only have to change them in one place. 
//Then you can just set them as variables in the test files. 

export const username = 'username';
export const password = 'password';
export const timeout = 15000;